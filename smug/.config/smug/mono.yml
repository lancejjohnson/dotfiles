session: mono

root: ~/src/dscout/mono

windows:
  - name: editor
    root: ./
    commands:
      - clear

  - name: terminal
    root: ./
    commands:
      - clear

  - name: agent
    root: ./
    commands:
      - clear
