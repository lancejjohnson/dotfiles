session: ${wt}

root: ${wtd}

windows:
  - name: editor
    root: ${wtd}/apps/axon
    commands:
      - clear
      - nvim

  - name: terminal
    root: ${wtd}/apps/axon
    commands:
      - clear

  - name: agent
    root: ${wtd}/apps/axon
    commands:
      - clear
      - claude
